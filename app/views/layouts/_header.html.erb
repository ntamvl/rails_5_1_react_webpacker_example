<nav class="navbar navbar-inverse navbar-toggleable-sm bg-success justify-content-between">
    <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target=".dual-nav">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="navbar-collapse collapse dual-nav">
        <ul class="navbar-nav">
            <li class="nav-item <%= request.path == "/" ? "active" : "" %>">
              <a class="nav-link pl-0" href="/"><i class="fa fa-home"></i> Home <span class="sr-only"></span></a>
            </li>
            <li class="nav-item <%= request.path == "/saved" ? "active" : "" %>">
              <a class="nav-link" href="#"><i class="fa fa-bookmark" aria-hidden="true"></i> Saved <span class="sr-only"></span></a>
            </li>
        </ul>
        <ul class="navbar-nav only-mobile hide">
            <% if current_user %>
            <li class="nav-item">
              <div class="nav-link">
                <i class="fa fa-user" aria-hidden="true"></i> Signed in as <strong><%= current_user[:name] %></strong>
              </div>
            </li>
            <li class="nav-item">
              <a href="<%= destroy_user_session_path %>" class="nav-link"> <i class="fa fa-sign-out" aria-hidden="true"></i> Sign out</a>
            </li>
          <% else %>
            <li class="nav-item">
              <a href="/auth/google_oauth2" class="nav-link"><i class="fa fa-sign-in" aria-hidden="true"></i> Sign in with Google</a>
            </li>
          <% end %>
        </ul>
    </div>
    <a href="/" class="navbar-brand mr-0">
      Affiliate Coin
    </a>
    <div class="navbar-collapse collapse dual-nav">
        <ul class="nav navbar-nav ml-auto">
            <% if current_user %>
            <li class="nav-item">
              <div class="nav-link">
                <i class="fa fa-user" aria-hidden="true"></i> Signed in as <strong><%= current_user[:name] %></strong>
              </div>
            </li>
            <li class="nav-item">
              <a rel="nofollow" class="nav-link" data-method="delete" href="<%= destroy_user_session_path %>">
                <i class="fa fa-sign-out" aria-hidden="true"></i> Logout</a>
            </li>
          <% else %>
            <li class="nav-item">
              <a href="<%= user_google_oauth2_omniauth_authorize_path %>" class="nav-link"><i class="fa fa-sign-in" aria-hidden="true"></i> Sign in with Google</a>
            </li>
          <% end %>
        </ul>
    </div>
</nav>
